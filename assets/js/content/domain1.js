/* network+ :: content/domain1.js — lazy-loaded reading module for N10-009 Domain 1 (deep). */
window.NETPLUS = window.NETPLUS || {}; NETPLUS.reading = NETPLUS.reading || {};
NETPLUS.reading[1] = [];
NETPLUS.reading[1].push(
{heading:"The OSI Reference Model: Layers and PDUs", body:"<p>The <span class='keyterm'>Open Systems Interconnection (OSI) reference model</span> is a seven-layer conceptual framework published by the ISO that describes how data moves from an application on one host, across a network, to an application on another host. No real-world stack implements OSI literally, but Network+ uses it as the universal vocabulary for troubleshooting and design. Every layer provides services to the layer above it and consumes services from the layer below, communicating with its peer layer on the remote host through <em>protocol data units (PDUs)</em>. This idea of <strong>peer-layer communication</strong> is central: Layer 3 on the sender logically talks to Layer 3 on the receiver, even though the actual bits travel all the way down to Layer 1, across the wire, and back up. Each layer behaves as if it has a direct conversation with its counterpart, ignoring the mechanics handled below it. This abstraction is what lets a web developer write HTTP code without knowing whether the underlying link is copper, fiber, or Wi-Fi.</p><h3>The seven layers, top to bottom</h3><p>The classic mnemonic <strong>\"All People Seem To Need Data Processing\"</strong> orders the layers 7 to 1. A common bottom-up alternative is <strong>\"Please Do Not Throw Sausage Pizza Away\"</strong> (Physical to Application). Each layer owns a distinct PDU and addressing scheme.</p><table><thead><tr><th>#</th><th>Layer</th><th>PDU</th><th>Key role / examples</th></tr></thead><tbody><tr><td>7</td><td>Application</td><td>Data</td><td>User-facing protocols: HTTP, DNS, SMTP, FTP</td></tr><tr><td>6</td><td>Presentation</td><td>Data</td><td>Encoding, encryption (TLS), compression, ASCII/Unicode, JPEG</td></tr><tr><td>5</td><td>Session</td><td>Data</td><td>Establishes, manages, tears down sessions; RPC, NetBIOS</td></tr><tr><td>4</td><td>Transport</td><td>Segment (TCP) / Datagram (UDP)</td><td>End-to-end delivery, ports, TCP/UDP, flow control</td></tr><tr><td>3</td><td>Network</td><td>Packet</td><td>Logical IP addressing, routing; IP, ICMP, routers</td></tr><tr><td>2</td><td>Data Link</td><td>Frame</td><td>MAC addressing, switching; Ethernet, ARP, switches</td></tr><tr><td>1</td><td>Physical</td><td>Bits</td><td>Cables, connectors, voltages, hubs, repeaters</td></tr></tbody></table><h3>Why PDUs matter</h3><p>The PDU name tells you which layer a device or protocol operates at. A <span class='keyterm'>frame</span> carries Layer 2 MAC addresses; a <span class='keyterm'>packet</span> carries Layer 3 IP addresses; a <span class='keyterm'>segment</span> carries Layer 4 port numbers. When an exam item says a switch \"forwards frames,\" that single word locates it at Layer 2. When a router \"routes packets,\" it is operating at Layer 3. This vocabulary is precise on purpose: if you hear \"datagram\" you should think UDP at Layer 4 (or sometimes the IP packet itself), and if you hear \"bits on the wire\" you are firmly at Layer 1. Memorizing the PDU lets you decode a question's intent from a single noun.</p><h3>What each layer actually contributes</h3><p>It helps to attach a concrete job to every layer rather than memorizing names alone. The <strong>Physical</strong> layer defines voltage levels, pin assignments, light pulses, and radio modulation, the raw signaling. The <strong>Data Link</strong> layer frames those bits, adds MAC source/destination, and detects errors with a frame check sequence. The <strong>Network</strong> layer assigns logical IP addresses and chooses paths between networks. The <strong>Transport</strong> layer multiplexes conversations with port numbers and decides reliability (TCP) versus speed (UDP). The <strong>Session</strong> layer opens, maintains, and closes dialogues, including checkpointing for long transfers. The <strong>Presentation</strong> layer translates formats, handling character encoding, compression, and encryption such as TLS. The <strong>Application</strong> layer is the protocol the program speaks, HTTP or SMTP, not the program itself, a distinction students frequently miss.</p><table><thead><tr><th>Concern</th><th>OSI layer</th><th>Why</th></tr></thead><tbody><tr><td>Encryption / TLS</td><td>6 (Presentation)</td><td>Translates/secures the data format</td></tr><tr><td>Port number filtering</td><td>4 (Transport)</td><td>Ports live in the TCP/UDP header</td></tr><tr><td>Routing / subnet mask</td><td>3 (Network)</td><td>Logical IP addressing decisions</td></tr><tr><td>VLAN / duplicate MAC</td><td>2 (Data Link)</td><td>MAC and frame switching</td></tr><tr><td>Bad cable / no link light</td><td>1 (Physical)</td><td>Signaling on the medium</td></tr></tbody></table><h3>The Layer 8 joke and split layers</h3><p>Technicians sometimes refer to \"Layer 8\" as the user, or to political and budget layers above seven. More usefully, remember that the data link layer is formally split into two sublayers: the <strong>LLC (Logical Link Control, 802.2)</strong> above and the <strong>MAC (Media Access Control)</strong> below, which is where the hardware burned-in address lives.</p><ul><li><strong>Upper layers (5-7)</strong> are largely software and are often grouped as the \"application\" portion in the simpler TCP/IP model.</li><li><strong>Lower layers (1-4)</strong> handle the mechanics of moving and addressing data and are the focus of most physical troubleshooting.</li></ul><h3>Common misconceptions</h3><p>Students often place TLS at Layer 7 because it secures web apps, but format translation and encryption are formally Layer 6. Another trap is assuming a router ignores Layers 1 and 2, it must process all lower layers to receive a frame before it can read the IP packet at Layer 3. Finally, the TCP/IP model collapses OSI's seven layers into four (Application, Transport, Internet, Link); know that the OSI Session, Presentation, and Application layers all fold into TCP/IP's single Application layer.</p><div class='callout exam'><div class='lbl'>Exam tip</div><p>Memorize the PDU-to-layer mapping cold: <strong>Bits, Frames, Packets, Segments, Data</strong> from Layer 1 up. The N10-009 objectives explicitly call out the OSI model, and many scenario questions hinge on matching a symptom or device to the correct layer. A bad cable is Layer 1; a duplicate MAC or VLAN issue is Layer 2; a routing or subnet-mask problem is Layer 3; a blocked port is Layer 4.</p></div><p>When troubleshooting, work the model methodically. Many seasoned engineers go <em>bottom-up</em>, confirming link lights and cabling (Layer 1) before suspecting IP configuration (Layer 3) or a firewall rule blocking a port (Layer 4). Others use <em>divide and conquer</em>, testing at Layer 3 with a ping first and then moving up or down based on the result. The OSI model is therefore not just trivia; it is a structured diagnostic checklist that prevents you from guessing randomly.</p>"},
{heading:"Encapsulation, Decapsulation, and Data Flow", body:"<p><span class='keyterm'>Encapsulation</span> is the process by which each OSI layer on the sending host wraps the data it receives from the layer above with its own header (and sometimes trailer) before handing it down. <span class='keyterm'>Decapsulation</span> is the mirror image on the receiving host: each layer strips off the header meant for it and passes the remaining payload up. This layered wrapping is what allows protocols at different layers to operate independently. A useful mental image is a set of nested envelopes: the application writes a letter, the transport layer seals it in an envelope marked with a port, the network layer puts that into a larger envelope marked with IP addresses, and the data link layer puts the whole thing into a shipping box stamped with MAC addresses. Each device along the way opens only the envelope it is responsible for.</p><h3>Building the packet on the way down</h3><p>Imagine a user clicking a link in a browser. The journey down the stack looks like this:</p><ol><li><strong>Application/Presentation/Session (7-5):</strong> The HTTP request (possibly TLS-encrypted at Layer 6) becomes the raw <em>data</em> payload.</li><li><strong>Transport (4):</strong> TCP prepends a header containing source and destination <strong>port numbers</strong> (e.g., destination 443), sequence and acknowledgment numbers. The result is a <em>segment</em>.</li><li><strong>Network (3):</strong> IP prepends a header with source and destination <strong>IP addresses</strong> and a TTL. The result is a <em>packet</em>.</li><li><strong>Data Link (2):</strong> Ethernet prepends a header with source and destination <strong>MAC addresses</strong> and appends a <em>frame check sequence (FCS)</em> trailer for error detection. The result is a <em>frame</em>.</li><li><strong>Physical (1):</strong> The frame is serialized into <em>bits</em> and signaled onto the wire as electrical, optical, or radio signals.</li></ol><table><thead><tr><th>Layer</th><th>Adds</th><th>Addressing</th></tr></thead><tbody><tr><td>Transport</td><td>TCP/UDP header</td><td>Port numbers</td></tr><tr><td>Network</td><td>IP header</td><td>IP addresses</td></tr><tr><td>Data Link</td><td>Frame header + FCS trailer</td><td>MAC addresses</td></tr></tbody></table><h3>What changes hop by hop</h3><p>A crucial concept: as a packet crosses routers, the <strong>Layer 2 frame is rebuilt at every hop</strong>. Source and destination MAC addresses change to reflect the current link, but the <strong>Layer 3 source and destination IP addresses remain constant</strong> end to end (barring NAT). This is why MAC addresses are called \"local\" and IP addresses \"logical\" or \"global.\" Think of IP as the final destination city printed on a package and MAC as the address of the next sorting facility, which is overwritten at every depot the package passes through.</p><table><thead><tr><th>Field</th><th>Changes at each hop?</th><th>Why</th></tr></thead><tbody><tr><td>Destination MAC</td><td>Yes</td><td>Points to the next-hop interface only</td></tr><tr><td>Source MAC</td><td>Yes</td><td>Set to the forwarding interface</td></tr><tr><td>Destination IP</td><td>No (barring NAT)</td><td>Identifies the final host end to end</td></tr><tr><td>TTL field</td><td>Decrements by 1</td><td>Loop prevention; 0 means discard</td></tr></tbody></table><div class='callout scenario'><div class='lbl'>Scenario</div><p>A host sends a packet to a server three routers away. At the first router, the destination MAC is the router's ingress interface, not the server. The router decapsulates to Layer 3, reads the destination IP, consults its routing table, then re-encapsulates with a new source MAC (its egress port) and a new destination MAC (the next-hop router). The IP header is untouched except TTL, which decrements by one. If TTL hits zero, the packet is dropped and an ICMP Time Exceeded is returned, the very mechanism traceroute exploits.</p></div><h3>MTU, fragmentation, and overhead</h3><p>The <span class='keyterm'>Maximum Transmission Unit (MTU)</span> defines the largest frame payload a link can carry, typically 1500 bytes for standard Ethernet. If a packet exceeds the path MTU, it may be fragmented at Layer 3 (IPv4) or dropped with an ICMP message instructing the sender to reduce size (IPv6 and path MTU discovery). <strong>Jumbo frames</strong> raise the MTU to around 9000 bytes, reducing per-packet overhead in storage and data-center networks.</p><ul><li>Each header is pure overhead, so efficiency improves with larger payloads.</li><li>Decapsulation on the receiver validates the FCS; a failed checksum silently discards the frame.</li><li>Fragmentation hurts performance because the receiver must reassemble pieces, and losing one fragment forces retransmission of the entire original packet.</li></ul><h3>Common misconceptions</h3><p>A frequent error is believing the source IP changes hop by hop, it does not, unless a NAT device deliberately rewrites it. Another is thinking encapsulation adds only headers; the data link layer also adds a trailer (the FCS). Students also confuse MTU with bandwidth, MTU is a size limit per frame, not a speed. Understanding encapsulation explains why a firewall filtering by port (Layer 4) must first decapsulate through Layers 1-3, and why a switch never looks at IP addresses while a router must.</p><div class='callout exam'><div class='lbl'>Exam tip</div><p>If a question asks what changes as a packet traverses routers, the answer is the <strong>MAC addresses and the TTL</strong>, while the source and destination <strong>IP stay the same</strong> (absent NAT). And remember the standard Ethernet MTU is <strong>1500 bytes</strong>; jumbo frames are about <strong>9000 bytes</strong>. A \"can't pass large packets but small pings work\" symptom points squarely at an MTU mismatch.</p></div>"},
{heading:"Networking Appliances: Routers, Switches, Hubs, and Bridges", body:"<p>Network+ expects you to place every common appliance at its correct OSI layer and to know precisely what forwarding decision it makes. The defining question is always: <em>what address does this device read, and what does it do with the traffic?</em> Two more vocabulary terms frame this entire topic. A <span class='keyterm'>collision domain</span> is the set of devices that could have their frames collide if they transmit at once; a <span class='keyterm'>broadcast domain</span> is the set of devices a single broadcast frame will reach. The whole evolution from hubs to switches to routers is the story of shrinking these domains to improve performance and segmentation.</p><h3>Hubs and repeaters (Layer 1)</h3><p>A <span class='keyterm'>hub</span> is a multiport repeater. It regenerates the electrical signal and floods every incoming bit out all other ports with no intelligence. All ports share one <strong>collision domain</strong> and one <strong>broadcast domain</strong>, forcing half-duplex operation and CSMA/CD. Because every device hears every transmission, a hub-based network degrades quickly under load as collisions multiply. Hubs are effectively obsolete but appear on exams as the contrast to switches. A <strong>repeater</strong> simply extends a cable run beyond its distance limit by regenerating the weakening signal.</p><h3>Bridges and switches (Layer 2)</h3><p>A <span class='keyterm'>bridge</span> connects two segments and learns MAC addresses to forward frames selectively, an early two-port ancestor of the switch. A <span class='keyterm'>switch</span> is a multiport bridge implemented in hardware (ASICs). It builds a <strong>MAC address table (CAM table)</strong> by learning source MACs, then forwards each frame only out the port where the destination lives. Key properties:</p><ul><li>Each switch port is its own <strong>collision domain</strong>, enabling full-duplex.</li><li>By default all ports share one <strong>broadcast domain</strong> unless segmented by VLANs.</li><li>Unknown-unicast and broadcast frames are flooded out all ports except the ingress.</li></ul><p>Switches make three forwarding decisions: <strong>forward</strong> (destination is known on a different port), <strong>filter</strong> (destination is on the same port the frame arrived on, so drop it), and <strong>flood</strong> (destination unknown or broadcast). They also run <strong>Spanning Tree Protocol (STP)</strong> to block redundant links and prevent the broadcast storms that physical loops would otherwise cause. A <strong>Layer 3 switch</strong> (multilayer switch) adds routing in hardware, performing inter-VLAN routing at wire speed while retaining switching functions.</p><h3>Routers (Layer 3)</h3><p>A <span class='keyterm'>router</span> connects different IP networks, reading destination IP addresses and consulting a routing table to choose the best next hop. Every router interface is a separate <strong>broadcast domain</strong> and separate <strong>collision domain</strong>. Routers do not forward broadcasts, which is why they segment networks. They also perform NAT, ACL filtering, and run routing protocols (OSPF, BGP, EIGRP). When no specific route matches, the router uses its <strong>default route (gateway of last resort)</strong>, which is how most traffic reaches the internet.</p><table><thead><tr><th>Device</th><th>OSI Layer</th><th>Reads</th><th>Collision domains</th><th>Broadcast domains</th></tr></thead><tbody><tr><td>Hub</td><td>1</td><td>Nothing (bits)</td><td>1 (shared)</td><td>1</td></tr><tr><td>Bridge</td><td>2</td><td>MAC</td><td>1 per port</td><td>1</td></tr><tr><td>Switch</td><td>2</td><td>MAC</td><td>1 per port</td><td>1 (or per VLAN)</td></tr><tr><td>Router</td><td>3</td><td>IP</td><td>1 per port</td><td>1 per port</td></tr></tbody></table><div class='callout exam'><div class='lbl'>Exam tip</div><p>Remember the two rules of segmentation: <strong>switches break up collision domains; routers break up broadcast domains.</strong> Count domains carefully in diagram questions. A 24-port switch with all ports in one VLAN has 24 collision domains but only 1 broadcast domain. Add a router and each routed interface becomes a new broadcast domain.</p></div><h3>Other appliances</h3><p>Wireless <strong>access points (APs)</strong> bridge wireless clients into the wired LAN at Layer 2. A <strong>wireless LAN controller (WLC)</strong> centrally manages many lightweight APs. <strong>Media converters</strong> translate between media types (copper to fiber) at Layer 1.</p><h3>Common misconceptions</h3><p>Students sometimes think a switch breaks up broadcast domains, it does not by default; only VLANs or routers do. Another trap is confusing a Layer 3 switch with a router: the Layer 3 switch routes between VLANs at high speed but typically lacks the WAN interfaces and rich feature set of a true router. Finally, do not assume an unmanaged switch can do VLANs, it cannot; VLAN segmentation requires a managed switch.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>Users on the same switch can reach each other but cannot reach the internet. Because intra-switch traffic (Layer 2) works, the problem is not the switch. Suspect the Layer 3 device: a misconfigured default gateway on the router, a missing route, or an ACL. This is classic OSI-layer reasoning, isolate which device must be involved by which addresses are failing.</p></div>"},
{heading:"Security & Service Appliances: Firewalls, IDS/IPS, Load Balancers, Proxies", body:"<p>Beyond the core forwarding devices, modern networks deploy specialized appliances for security and service delivery. Network+ requires you to know what each does, where it sits, and how it differs from its near-neighbors. A recurring theme is <em>placement</em>: whether a device sits <strong>inline</strong> (traffic must pass through it, so it can block) or <strong>out-of-band</strong> (it receives a copy, so it can only observe). Knowing the placement of each appliance answers most exam questions about what it can and cannot do.</p><h3>Firewalls</h3><p>A <span class='keyterm'>firewall</span> enforces traffic policy between security zones (e.g., the untrusted internet and a trusted LAN). Generations include:</p><ul><li><strong>Stateless / packet-filtering:</strong> matches individual packets against ACLs by IP, port, and protocol.</li><li><strong>Stateful:</strong> tracks connection state in a session table, allowing return traffic for established flows automatically.</li><li><strong>Next-generation firewall (NGFW):</strong> adds deep packet inspection, application awareness (Layer 7), user identity, and integrated IPS.</li></ul><p>Firewalls commonly create a <span class='keyterm'>screened subnet</span> (formerly DMZ) for internet-facing servers, isolating them from the internal LAN. Firewall rules are typically processed <strong>top-down with an implicit deny at the end</strong>, so rule order matters: a permissive rule placed above a restrictive one can unintentionally allow traffic you meant to block.</p><h3>IDS vs IPS</h3><p>An <span class='keyterm'>Intrusion Detection System (IDS)</span> monitors traffic and <strong>alerts</strong> on suspicious activity but does not block it, typically deployed out-of-band on a SPAN/mirror port. An <span class='keyterm'>Intrusion Prevention System (IPS)</span> sits <strong>inline</strong> and can actively drop malicious traffic. Both use signature-based and anomaly-based detection. The trade-off is real: an IPS can stop an attack mid-stream but introduces latency and becomes a potential single point of failure, while an IDS never delays traffic but only raises an alarm after the fact.</p><table><thead><tr><th>Feature</th><th>IDS</th><th>IPS</th></tr></thead><tbody><tr><td>Placement</td><td>Out-of-band (passive)</td><td>Inline (active)</td></tr><tr><td>Action</td><td>Detect and alert</td><td>Detect and block</td></tr><tr><td>Latency risk</td><td>None to traffic</td><td>Adds inline latency</td></tr><tr><td>Failure impact</td><td>Monitoring lost</td><td>Can disrupt traffic</td></tr></tbody></table><h3>Load balancers</h3><p>A <span class='keyterm'>load balancer</span> distributes incoming requests across a pool of backend servers to improve performance and availability. Layer 4 load balancers decide by IP and port; Layer 7 (application) load balancers can route by URL, cookie, or HTTP header. Common methods include <strong>round robin, least connections,</strong> and <strong>weighted</strong> distribution. They also perform health checks and can terminate TLS (SSL offload). Many also support <strong>session persistence (sticky sessions)</strong>, pinning a user to the same backend so shopping carts and login state survive across requests.</p><h3>Proxies</h3><p>A <span class='keyterm'>forward proxy</span> sits in front of internal clients, making outbound requests on their behalf for caching, content filtering, and anonymity. A <span class='keyterm'>reverse proxy</span> sits in front of servers, accepting inbound requests and forwarding them to the right backend, often combined with load balancing, caching, and TLS termination.</p><table><thead><tr><th>Appliance</th><th>Protects/serves</th><th>Placement</th></tr></thead><tbody><tr><td>Forward proxy</td><td>Clients (outbound)</td><td>In front of users</td></tr><tr><td>Reverse proxy</td><td>Servers (inbound)</td><td>In front of server farm</td></tr><tr><td>Load balancer</td><td>Server pool availability</td><td>In front of backends</td></tr><tr><td>IDS</td><td>Visibility</td><td>Out-of-band (SPAN)</td></tr><tr><td>IPS / firewall</td><td>Enforcement</td><td>Inline</td></tr></tbody></table><div class='callout exam'><div class='lbl'>Exam tip</div><p>Distinguish the proxies by direction: a <strong>forward proxy protects/serves clients</strong> (employees browsing out); a <strong>reverse proxy protects/serves servers</strong> (the public reaching your web farm). And remember the IDS/IPS one-liner: <strong>IDS detects, IPS prevents.</strong> If the question says traffic was logged but still reached the target, it was an IDS; if it was dropped, an IPS.</p></div><h3>Additional appliances and a misconception</h3><ul><li><strong>VPN concentrator / headend:</strong> terminates many remote-access or site-to-site VPN tunnels (IPsec, SSL).</li><li><strong>NAS / SAN:</strong> network-attached storage (file-level, e.g., SMB/NFS) versus storage area network (block-level, e.g., iSCSI, Fibre Channel).</li></ul><p>A common misconception is that NAT or a load balancer is a firewall, neither enforces a security policy by itself. Another is confusing a load balancer with a reverse proxy; they overlap heavily and are often the same box, but a pure load balancer focuses on distribution and health while a reverse proxy adds caching, compression, and request rewriting.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>A retailer's web app slows to a crawl during a flash sale and one server crashes. A load balancer with health checks would have detected the failed node, stopped sending it traffic, and spread load across healthy servers, preserving availability. Pairing it with a reverse proxy for caching static assets reduces backend load further.</p></div>"},
{heading:"Common Ports and Protocols I: Management & File Transfer", body:"<p>Memorizing well-known port numbers is non-negotiable for Network+. Ports 0-1023 are <span class='keyterm'>well-known ports</span> assigned by IANA; 1024-49151 are registered; 49152-65535 are dynamic/ephemeral. The ephemeral range is what a client uses as its <em>source</em> port when it opens a connection, which is why the same browser can hold dozens of simultaneous sessions to port 443, each distinguished by a different high-numbered source port. This section covers remote management and file transfer; the next covers web, mail, naming, and time.</p><h3>Remote management protocols</h3><table><thead><tr><th>Protocol</th><th>Port</th><th>Transport</th><th>Purpose</th></tr></thead><tbody><tr><td>SSH</td><td>22</td><td>TCP</td><td>Encrypted remote CLI, secure tunneling</td></tr><tr><td>Telnet</td><td>23</td><td>TCP</td><td>Legacy remote CLI, plaintext (insecure)</td></tr><tr><td>RDP</td><td>3389</td><td>TCP/UDP</td><td>Remote Desktop (Windows GUI)</td></tr><tr><td>SNMP</td><td>161/162</td><td>UDP</td><td>Device monitoring; 161 polling, 162 traps</td></tr><tr><td>Syslog</td><td>514</td><td>UDP</td><td>Centralized log collection</td></tr></tbody></table><p><span class='keyterm'>SSH (22)</span> is the modern standard, replacing Telnet because it encrypts the entire session including credentials. <strong>Telnet (23)</strong> survives only on legacy gear and should be flagged as a security risk because anyone capturing the traffic can read passwords in clear text. <strong>RDP (3389)</strong> provides full graphical remote control of Windows hosts and is a frequent target of brute-force attacks, so it should never be exposed directly to the internet. <span class='keyterm'>SNMP</span> uses 161 for the manager to poll agents and 162 for agents to send unsolicited <em>traps</em>; SNMPv3 adds authentication and encryption, unlike v1/v2c which use plaintext community strings. <strong>Syslog (514)</strong> centralizes log messages from many devices to one collector for correlation and retention.</p><h3>File transfer protocols</h3><table><thead><tr><th>Protocol</th><th>Port</th><th>Transport</th><th>Notes</th></tr></thead><tbody><tr><td>FTP</td><td>20/21</td><td>TCP</td><td>21 control, 20 data; plaintext</td></tr><tr><td>FTPS</td><td>989/990</td><td>TCP</td><td>FTP over TLS/SSL</td></tr><tr><td>SFTP</td><td>22</td><td>TCP</td><td>File transfer over SSH</td></tr><tr><td>TFTP</td><td>69</td><td>UDP</td><td>Trivial FTP, no auth; config/image transfer</td></tr><tr><td>SMB</td><td>445</td><td>TCP</td><td>Windows file/printer sharing</td></tr></tbody></table><p>Be careful to separate the look-alikes. <strong>FTP</strong> uses two ports: 21 for commands and 20 for active-mode data. <span class='keyterm'>SFTP</span> is <em>not</em> FTP at all, it is a subsystem of SSH and rides on <strong>port 22</strong>. <strong>FTPS</strong> is genuine FTP wrapped in TLS, using ports 989/990. <strong>TFTP (69, UDP)</strong> is extremely lightweight with no authentication, ideal for PXE boot and pushing router/switch firmware on a trusted LAN. <strong>SMB (445)</strong> handles Windows file and printer sharing and is heavily targeted by worms, which is why port 445 is almost always blocked at the internet edge.</p><h3>Secure vs insecure pairs</h3><table><thead><tr><th>Insecure</th><th>Port</th><th>Secure replacement</th><th>Port</th></tr></thead><tbody><tr><td>Telnet</td><td>23</td><td>SSH</td><td>22</td></tr><tr><td>FTP</td><td>20/21</td><td>SFTP / FTPS</td><td>22 / 989-990</td></tr><tr><td>SNMPv1/v2c</td><td>161/162</td><td>SNMPv3</td><td>161/162</td></tr></tbody></table><div class='callout exam'><div class='lbl'>Exam tip</div><p>The single most-confused pair on the exam is <strong>SFTP vs FTPS</strong>. SFTP = SSH File Transfer Protocol = <strong>port 22</strong>, one connection, secured by SSH. FTPS = FTP Secure = FTP + TLS = <strong>ports 989/990</strong>, multiple connections. If a question mentions SSH keys, the answer is SFTP; if it mentions certificates, lean FTPS.</p></div><h3>Directory and authentication ports</h3><ul><li><strong>LDAP 389 (TCP/UDP):</strong> directory queries (Active Directory).</li><li><strong>LDAPS 636 (TCP):</strong> LDAP over TLS.</li><li><strong>Kerberos 88:</strong> ticket-based authentication.</li><li><strong>RADIUS 1812/1813 (UDP):</strong> AAA authentication and accounting.</li><li><strong>TACACS+ 49 (TCP):</strong> Cisco AAA, encrypts the full payload.</li></ul><p>A common misconception is that SFTP and FTPS are interchangeable names, they are entirely different protocols built on SSH and TLS respectively. Another is assuming TFTP is just a faster FTP; it has no authentication and no directory listing, so it is only appropriate on trusted segments.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>A network admin needs to back up a switch's running configuration to a server on the same management VLAN with minimal setup. TFTP (UDP 69) is the classic choice for pushing and pulling device images and configs. For transfers across untrusted links, the admin should instead use SCP or SFTP over SSH (port 22) so credentials and data are encrypted.</p></div>"},
{heading:"Common Ports and Protocols II: Web, Mail, Naming, and Time", body:"<p>This section completes the port memorization set with the protocols that power web browsing, email, name resolution, address assignment, and time synchronization. These appear constantly in firewall and troubleshooting scenarios, because a single misconfigured port can break an otherwise healthy network. Treat each protocol below as both a number to memorize and a service whose failure produces a recognizable symptom.</p><h3>Web protocols</h3><table><thead><tr><th>Protocol</th><th>Port</th><th>Transport</th><th>Use</th></tr></thead><tbody><tr><td>HTTP</td><td>80</td><td>TCP</td><td>Unencrypted web</td></tr><tr><td>HTTPS</td><td>443</td><td>TCP</td><td>TLS-encrypted web</td></tr></tbody></table><p><span class='keyterm'>HTTPS (443)</span> is HTTP secured by TLS and is now the default for virtually all web traffic; <strong>HTTP (80)</strong> remains for redirects and internal sites. QUIC/HTTP3 also uses UDP 443. When you browse to a site over HTTPS, the TLS handshake on port 443 negotiates encryption keys and validates the server's certificate before any page content flows, which is why an expired certificate produces a browser warning even though the port itself is open.</p><h3>Email protocols</h3><table><thead><tr><th>Protocol</th><th>Plain port</th><th>Secure port</th><th>Function</th></tr></thead><tbody><tr><td>SMTP</td><td>25</td><td>587 (submission) / 465</td><td>Sending mail between servers</td></tr><tr><td>POP3</td><td>110</td><td>995</td><td>Download mail (removes from server)</td></tr><tr><td>IMAP</td><td>143</td><td>993</td><td>Sync mail (keeps on server)</td></tr></tbody></table><p><strong>SMTP (25)</strong> moves mail between servers; clients submit outbound mail on <strong>587</strong> (with STARTTLS). <span class='keyterm'>POP3 (110/995)</span> typically downloads and deletes mail, suiting single-device use. <span class='keyterm'>IMAP (143/993)</span> keeps mail on the server and synchronizes folders across devices, the modern default. A practical way to remember the difference: <strong>POP pulls mail off</strong> the server, so a second device sees nothing, while <strong>IMAP keeps everything in sync</strong>, so your phone and laptop show the same inbox. Port 25 is frequently blocked by ISPs to curb spam, which is why mail clients use 587 for submission.</p><h3>Naming, addressing, and time</h3><table><thead><tr><th>Protocol</th><th>Port</th><th>Transport</th><th>Role</th></tr></thead><tbody><tr><td>DNS</td><td>53</td><td>UDP/TCP</td><td>Name to IP resolution</td></tr><tr><td>DHCP</td><td>67/68</td><td>UDP</td><td>Automatic IP assignment</td></tr><tr><td>NTP</td><td>123</td><td>UDP</td><td>Time synchronization</td></tr></tbody></table><p><span class='keyterm'>DNS (53)</span> uses UDP for ordinary queries but switches to <strong>TCP for zone transfers</strong> and responses larger than 512 bytes (DNSSEC, EDNS). <span class='keyterm'>DHCP</span> uses <strong>server port 67 and client port 68</strong>; remember the DORA exchange (Discover, Offer, Request, Acknowledge). <strong>NTP (123)</strong> synchronizes clocks, which is critical for Kerberos authentication, certificate validation, and accurate logs. If clocks drift more than a few minutes, Kerberos tickets are rejected and TLS certificates may appear invalid, so a broken NTP can manifest as a mysterious authentication failure.</p><h3>Symptom-to-port quick reference</h3><table><thead><tr><th>Symptom</th><th>Likely culprit</th><th>Port</th></tr></thead><tbody><tr><td>Can ping IP, names fail</td><td>DNS</td><td>53</td></tr><tr><td>Host has APIPA 169.254 address</td><td>DHCP</td><td>67/68</td></tr><tr><td>Kerberos/cert errors network-wide</td><td>NTP time drift</td><td>123</td></tr><tr><td>Web works but not secure web</td><td>HTTPS/TLS</td><td>443</td></tr></tbody></table><div class='callout exam'><div class='lbl'>Exam tip</div><p>Watch the secure-port pairings: <strong>POP3 110 to 995, IMAP 143 to 993, HTTP 80 to 443, LDAP 389 to 636.</strong> The secure variant is almost never adjacent to the plain port number, so memorize the pairs as units. Also lock in DNS = 53 and DHCP = 67/68; these two underpin nearly every \"can't reach the internet\" scenario.</p></div><div class='callout scenario'><div class='lbl'>Scenario</div><p>A user can ping <strong>8.8.8.8</strong> but cannot browse to websites by name. Connectivity (Layer 3) is fine, so the failure is name resolution. The DNS server (UDP 53) is unreachable or misconfigured in the client's IP settings. Confirm with <em>nslookup</em>; if the IP works but the hostname fails, DNS is the culprit, not routing.</p></div><h3>Other notable ports</h3><ul><li><strong>SQL Server 1433, MySQL 3306:</strong> database connections.</li><li><strong>H.323 1720, SIP 5060/5061:</strong> VoIP signaling.</li><li><strong>GRE (protocol 47), IPsec (ESP 50, AH 51, IKE UDP 500):</strong> tunneling and VPNs.</li></ul><p>Treat these tables as flashcards. On test day, recalling port-to-protocol mappings instantly frees mental bandwidth for the reasoning portions of scenario questions.</p>"},
{heading:"TCP vs UDP and Traffic Types", body:"<p>At the transport layer (Layer 4), two protocols dominate: <span class='keyterm'>TCP</span> and <span class='keyterm'>UDP</span>. Choosing between them is a fundamental trade-off between reliability and speed, and Network+ tests both the mechanics and the appropriate use cases. The transport layer's job is to take the single pipe between two hosts and split it into many independent conversations using <strong>port numbers</strong>, a process called multiplexing. TCP and UDP are simply two different contracts for how reliably those conversations are delivered.</p><h3>TCP: connection-oriented and reliable</h3><p>The <span class='keyterm'>Transmission Control Protocol</span> is connection-oriented. Before any data flows, it establishes a session with the <strong>three-way handshake</strong>:</p><ol><li><strong>SYN</strong> - client proposes a connection and an initial sequence number.</li><li><strong>SYN-ACK</strong> - server acknowledges and sends its own sequence number.</li><li><strong>ACK</strong> - client acknowledges; the connection is established.</li></ol><p>TCP provides sequencing, acknowledgments, retransmission of lost segments, flow control (sliding window), and graceful teardown (FIN/ACK). This reliability adds overhead and latency but guarantees ordered, complete delivery, ideal for web pages, email, and file transfer. The <strong>sliding window</strong> lets TCP send multiple segments before waiting for an acknowledgment, and it shrinks or grows the window to match what the receiver and the network can handle, the basis of congestion control. A half-open connection stuck after only the SYN is the signature of a SYN-flood denial-of-service attack.</p><h3>UDP: connectionless and fast</h3><p>The <span class='keyterm'>User Datagram Protocol</span> is connectionless: it fires datagrams with no handshake, no acknowledgments, and no retransmission. It is often called \"best-effort\" or \"fire and forget.\" The lack of overhead makes it ideal for real-time and high-volume applications where speed matters more than perfect delivery and where retransmitting old data would be useless. For a live voice call, a packet that arrives late is worse than one that never arrives, so UDP simply drops it and moves on, and the application conceals the gap.</p><table><thead><tr><th>Characteristic</th><th>TCP</th><th>UDP</th></tr></thead><tbody><tr><td>Connection</td><td>Connection-oriented</td><td>Connectionless</td></tr><tr><td>Handshake</td><td>Three-way</td><td>None</td></tr><tr><td>Reliability</td><td>Guaranteed, retransmits</td><td>Best-effort</td></tr><tr><td>Ordering</td><td>Sequenced</td><td>Unordered</td></tr><tr><td>Header size</td><td>20+ bytes</td><td>8 bytes</td></tr><tr><td>Speed</td><td>Slower</td><td>Faster</td></tr><tr><td>Examples</td><td>HTTP, SSH, FTP, SMTP</td><td>DNS, DHCP, VoIP, streaming, TFTP</td></tr></tbody></table><div class='callout exam'><div class='lbl'>Exam tip</div><p>Map protocols to their transport. Mostly UDP: <strong>DHCP (67/68), TFTP (69), SNMP (161/162), NTP (123), Syslog (514)</strong>, and real-time voice/video. DNS (53) uses <strong>UDP for queries, TCP for zone transfers</strong>. If a scenario describes choppy VoIP, the right fix is QoS prioritization, not switching to TCP, because retransmitting late voice packets would only worsen the call.</p></div><h3>Traffic types</h3><p>Beyond transport choice, you must classify how traffic is addressed to receivers:</p><ul><li><span class='keyterm'>Unicast</span> - one sender to one specific receiver (the vast majority of traffic).</li><li><span class='keyterm'>Broadcast</span> - one sender to <em>all</em> hosts in the broadcast domain (e.g., ARP requests, sent to 255.255.255.255). IPv6 has no broadcast.</li><li><span class='keyterm'>Multicast</span> - one sender to a <em>group</em> of interested receivers (IP range 224.0.0.0/4), used by streaming, IPTV, and routing protocols like OSPF.</li><li><span class='keyterm'>Anycast</span> - one sender to the <em>nearest</em> of several hosts sharing one address; common for global DNS roots and CDNs.</li></ul><table><thead><tr><th>Type</th><th>Receivers</th><th>Example</th></tr></thead><tbody><tr><td>Unicast</td><td>One specific host</td><td>Web request to one server</td></tr><tr><td>Broadcast</td><td>All hosts on segment</td><td>ARP, DHCP Discover</td></tr><tr><td>Multicast</td><td>Subscribed group</td><td>IPTV, OSPF updates</td></tr><tr><td>Anycast</td><td>Nearest of many</td><td>DNS root servers, CDN</td></tr></tbody></table><h3>Common misconceptions</h3><p>A frequent error is thinking UDP is unreliable because it is broken, it deliberately trades reliability for speed, and the application layer can add its own checks if needed. Another is believing IPv6 still uses broadcast; it replaces broadcast entirely with multicast and anycast. Students also assume the three-way handshake transfers data, it does not, it only synchronizes sequence numbers before the real data begins.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>An IPTV deployment streams a live channel to 500 employees. Sending 500 separate unicast copies would saturate the core. Multicast lets the server transmit one stream that routers replicate only toward subnets with subscribed viewers, conserving bandwidth dramatically. This is exactly why multicast exists and why exam questions about efficient one-to-many delivery point to it.</p></div><p>Understanding both the transport trade-off and the addressing model lets you predict bandwidth behavior and choose the correct protocol for any application a question describes.</p>"},
{heading:"Transmission Media: Copper Cabling and Categories", body:"<p>The physical layer (Layer 1) carries bits as signals over a medium. Copper twisted-pair remains the workhorse of LAN access, and Network+ demands precise knowledge of cable categories, their speeds, distances, and construction. Copper carries data as changing voltage, which makes it cheap and easy to terminate but also vulnerable to two enemies: <strong>attenuation</strong> (signal weakening over distance) and <strong>interference</strong> (EMI and crosstalk corrupting the signal). Every cable category is essentially an engineering answer to pushing more bits faster while holding those two enemies at bay.</p><h3>Twisted pair fundamentals</h3><p>Ethernet copper uses four twisted pairs. <strong>Twisting</strong> cancels electromagnetic interference (EMI) and crosstalk; tighter twists and better insulation yield higher categories. Two shielding philosophies exist:</p><ul><li><span class='keyterm'>UTP (Unshielded Twisted Pair)</span> - no shielding, cheaper and easier to terminate; standard for offices.</li><li><span class='keyterm'>STP / shielded variants (F/UTP, S/FTP)</span> - foil or braid shielding for high-EMI environments like factories.</li></ul><p>Connectors are <strong>RJ45 (8P8C)</strong> for data; the older <strong>RJ11</strong> is for telephone. Wiring follows <strong>T568A</strong> or <strong>T568B</strong> pinouts; matching ends make a straight-through cable, mixing A and B makes a crossover (largely obsolete thanks to Auto-MDIX). Two crosstalk terms appear on the exam: <strong>NEXT (near-end crosstalk)</strong>, measured at the transmitting end, and <strong>FEXT (far-end crosstalk)</strong>, measured at the far end. Higher categories suppress both, partly through tighter, more varied twist rates per pair.</p><h3>Category comparison</h3><table><thead><tr><th>Category</th><th>Max speed</th><th>Bandwidth</th><th>Max distance</th><th>Notes</th></tr></thead><tbody><tr><td>Cat5</td><td>100 Mbps</td><td>100 MHz</td><td>100 m</td><td>Legacy, Fast Ethernet</td></tr><tr><td>Cat5e</td><td>1 Gbps</td><td>100 MHz</td><td>100 m</td><td>Reduced crosstalk; common baseline</td></tr><tr><td>Cat6</td><td>1 Gbps (10G to 55 m)</td><td>250 MHz</td><td>100 m / 55 m at 10G</td><td>Tighter twists</td></tr><tr><td>Cat6a</td><td>10 Gbps</td><td>500 MHz</td><td>100 m</td><td>10G full distance; often shielded</td></tr><tr><td>Cat7</td><td>10 Gbps</td><td>600 MHz</td><td>100 m</td><td>Fully shielded (S/FTP); GG45/TERA</td></tr><tr><td>Cat8</td><td>25-40 Gbps</td><td>2000 MHz</td><td>30 m</td><td>Data center top-of-rack only</td></tr></tbody></table><p>The pattern to internalize: every category supports <strong>100 meters</strong> except where 10G pushes the limit, <strong>Cat6 hits 10G only to 55 m</strong>, and <strong>Cat8 is capped at 30 m</strong> for short data-center runs. <strong>Cat6a</strong> is the sweet spot for 10GBASE-T across a full 100 m channel. Note that bandwidth in MHz is the signaling frequency, not the data rate; higher MHz allows more bits to be encoded per second, which is why Cat6a's 500 MHz enables full-distance 10G where Cat6's 250 MHz cannot.</p><h3>Ethernet standards naming</h3><table><thead><tr><th>Standard</th><th>Speed</th><th>Minimum cable</th></tr></thead><tbody><tr><td>100BASE-TX</td><td>100 Mbps</td><td>Cat5</td></tr><tr><td>1000BASE-T</td><td>1 Gbps</td><td>Cat5e</td></tr><tr><td>10GBASE-T</td><td>10 Gbps</td><td>Cat6a (Cat6 to 55 m)</td></tr></tbody></table><p>Decode the naming: the number is the speed in Mbps, <strong>BASE</strong> means baseband signaling, and the suffix (T) means twisted-pair copper. This lets you read 10GBASE-T as \"10 Gbps baseband over twisted pair.\"</p><h3>Other copper standards</h3><ul><li><strong>Coaxial:</strong> RG-6 (cable internet/CATV, F-connector) and RG-59 (older video, shorter runs).</li><li><strong>DAC (Direct Attach Copper):</strong> twinaxial assemblies with fixed SFP+ ends for cheap, short data-center links.</li><li><strong>Plenum vs PVC:</strong> plenum-rated (CMP) jackets are fire-resistant and low-smoke, required in air-handling spaces; PVC (riser/CMR) is cheaper but unsuitable for plenum areas.</li></ul><p>A common misconception is that a higher category is always better regardless of cost, in practice you match the category to the required speed and distance, since Cat6a costs more and is harder to terminate than Cat5e. Another is forgetting that the 100 m limit is the total channel including patch cords, not just the horizontal run.</p><div class='callout exam'><div class='lbl'>Exam tip</div><p>Three distance numbers win points: <strong>100 m</strong> for standard twisted pair, <strong>55 m</strong> for 10G over Cat6, and <strong>30 m</strong> for Cat8. Also know that <strong>Cat6a is the minimum category guaranteeing 10GBASE-T at the full 100 m.</strong> If a question asks what cable is needed in a noisy factory, the answer is a shielded type (STP/Cat6a/Cat7), not UTP.</p></div><div class='callout scenario'><div class='lbl'>Scenario</div><p>An installer runs cable above a drop ceiling that doubles as an HVAC return air plenum. Local fire code requires <strong>plenum-rated (CMP)</strong> cable so that, if it burns, it does not release toxic smoke into the air-circulation system. Choosing cheaper PVC here would be a code violation, a frequent exam trap that tests safety knowledge alongside category specs.</p></div>"},
{heading:"Transmission Media: Fiber, Connectors, and Transceivers", body:"<p>Optical fiber transmits data as pulses of light through a glass or plastic core, offering enormous bandwidth, long distances, and complete immunity to EMI. Network+ expects you to distinguish the two fiber types, recognize connectors, and match transceivers to roles. Because fiber carries light rather than electricity, it neither radiates a signal that can be tapped easily nor picks up interference from motors, fluorescent lights, or lightning, making it the default for backbone links, inter-building runs, and any high-EMI or high-security environment.</p><h3>Single-mode vs multimode</h3><p>The defining difference is core size and how light propagates:</p><ul><li><span class='keyterm'>Single-mode fiber (SMF)</span> has a tiny ~9-micron core that carries a single light path, usually from a <strong>laser</strong>. It supports the longest distances (tens of kilometers) and highest bandwidth. Jackets are conventionally <strong>yellow</strong>.</li><li><span class='keyterm'>Multimode fiber (MMF)</span> has a larger 50 or 62.5-micron core allowing many light paths (modes), driven by cheaper <strong>LEDs or VCSELs</strong>. Modal dispersion limits distance. Jackets are <strong>orange (OM1/OM2)</strong> or <strong>aqua (OM3/OM4)</strong>.</li></ul><p>The key mechanism is <strong>modal dispersion</strong>: in multimode fiber the many light paths arrive at slightly different times, smearing the signal and limiting distance, whereas single-mode's narrow core permits only one path, so the pulse stays crisp over tens of kilometers. This is why long-haul links are always single-mode despite its more expensive laser optics.</p><table><thead><tr><th>Type</th><th>Core</th><th>Typical reach (10G)</th><th>Color</th></tr></thead><tbody><tr><td>OM1</td><td>62.5 um</td><td>~33 m</td><td>Orange</td></tr><tr><td>OM2</td><td>50 um</td><td>~82 m</td><td>Orange</td></tr><tr><td>OM3</td><td>50 um</td><td>~300 m</td><td>Aqua</td></tr><tr><td>OM4</td><td>50 um</td><td>~400 m</td><td>Aqua</td></tr><tr><td>OS1/OS2</td><td>9 um (SMF)</td><td>10-40+ km</td><td>Yellow</td></tr></tbody></table><p><strong>OS1</strong> is rated for indoor/tight-buffered runs while <strong>OS2</strong> (loose-tube) is for long outdoor/metro spans. Among multimode grades, higher OM numbers mean greater bandwidth and reach; <strong>OM3 and OM4</strong> are the data-center standards for 10G/40G/100G.</p><h3>Connectors</h3><ul><li><span class='keyterm'>LC (Lucent Connector)</span> - small form-factor, push-pull, the modern duplex standard in SFP modules.</li><li><span class='keyterm'>SC (Subscriber Connector)</span> - square \"stick and click\" push-pull.</li><li><span class='keyterm'>ST (Straight Tip)</span> - round bayonet \"twist\" connector, common on older MMF.</li><li><strong>MPO/MTP</strong> - multi-fiber ribbon connector for 40G/100G parallel optics.</li></ul><p>Polish types matter for reflection: <strong>UPC (blue)</strong> for standard, <strong>APC (green)</strong> with an angled ferrule for the lowest back-reflection. Never mate APC to UPC. A handy memory aid: <strong>LC is Little</strong> (small form factor), <strong>SC is Square and uses a Stick-and-Click</strong> motion, and <strong>ST is a Straight Tip you Twist</strong> to lock.</p><h3>Transceivers</h3><p>Pluggable optics let one switch port support many media. Know the form factors and their typical speeds:</p><table><thead><tr><th>Transceiver</th><th>Typical speed</th></tr></thead><tbody><tr><td>SFP</td><td>1 Gbps</td></tr><tr><td>SFP+</td><td>10 Gbps</td></tr><tr><td>SFP28</td><td>25 Gbps</td></tr><tr><td>QSFP+</td><td>40 Gbps</td></tr><tr><td>QSFP28</td><td>100 Gbps</td></tr></tbody></table><p>The <strong>Q</strong> in QSFP stands for \"Quad,\" bundling four lanes. <strong>BiDi</strong> optics use one strand with two wavelengths; <strong>GBIC</strong> is the older, larger predecessor to SFP. Transceivers are hot-swappable and let the same chassis serve copper DAC, multimode, or single-mode simply by swapping the module, which is why modern switches ship with empty SFP cages rather than fixed fiber ports.</p><h3>Fiber vs copper at a glance</h3><table><thead><tr><th>Property</th><th>Copper</th><th>Fiber</th></tr></thead><tbody><tr><td>Distance</td><td>Up to 100 m</td><td>Hundreds of m to tens of km</td></tr><tr><td>EMI immunity</td><td>Susceptible</td><td>Immune</td></tr><tr><td>Cost/termination</td><td>Cheap, easy</td><td>Costlier, skilled</td></tr><tr><td>Security</td><td>Easier to tap</td><td>Hard to tap</td></tr></tbody></table><p>A common misconception is that multimode is \"better\" because it sounds more advanced; in fact single-mode reaches much farther. Another is assuming any transceiver fits any fiber, a single-mode SFP must pair with single-mode fiber and a multimode module with multimode fiber, or the link will fail or perform poorly.</p><div class='callout exam'><div class='lbl'>Exam tip</div><p>Anchor on the color and core cues: <strong>single-mode = yellow, ~9 um, long-distance laser; multimode = orange/aqua, 50/62.5 um, short-distance LED.</strong> For speed, climb the ladder <strong>SFP (1G) to SFP+ (10G) to SFP28 (25G) to QSFP+ (40G) to QSFP28 (100G).</strong> If a scenario needs a 30 km link between buildings, only single-mode fiber qualifies, copper and multimode fall far short.</p></div><div class='callout scenario'><div class='lbl'>Scenario</div><p>A campus connects two buildings 800 meters apart. Cat6a tops out at 100 m and multimode OM4 reaches about 400 m at 10G, so both fail. The correct solution is <strong>single-mode fiber with SFP+ long-reach transceivers</strong>, which easily spans the distance while remaining immune to the outdoor EMI and lightning-induced surges that plague copper between structures.</p></div>"}
);

NETPLUS.reading[1].push(
  {heading:"Network Topologies and Architectures", body:"<p>A <span class='keyterm'>topology</span> describes how nodes and links are arranged. Network+ distinguishes <strong>physical topology</strong> (the literal cabling and device placement) from <strong>logical topology</strong> (the path data actually travels). A network can be wired as a physical star yet behave as a logical bus, so always read the question carefully to determine which view is being tested. The classic example is legacy 10BASE-T: every station plugs into a central hub (a physical star), but because the hub repeats every bit out all ports, the electrical behavior is a shared logical bus where all stations contend for one collision domain. Modern switched Ethernet keeps the physical star but changes the logical behavior, since a switch forwards each frame only toward its destination.</p><h3>Classic physical topologies</h3><ul><li><strong>Star</strong> &mdash; every node connects to a central switch or hub. Dominant in modern Ethernet LANs. A single cable fault isolates one host; a switch failure takes down the segment. Easy to troubleshoot and expand because each run is independent and faults are localized to one drop.</li><li><strong>Bus</strong> &mdash; all nodes share one backbone cable terminated at both ends. Legacy (10BASE2/5). A single break or missing terminator brings down the whole segment, and signal reflection from an unterminated end corrupts traffic for everyone. Cheap to cable but a troubleshooting nightmare.</li><li><strong>Ring</strong> &mdash; each node connects to two neighbors forming a loop (Token Ring, FDDI, SONET). Access is deterministic because a token grants permission to transmit, eliminating collisions. Dual-ring designs self-heal by wrapping traffic onto the backup ring when a fiber breaks.</li><li><strong>Mesh</strong> &mdash; nodes interconnect directly. A <em>full mesh</em> of n nodes needs n(n-1)/2 links and offers maximum redundancy; a <em>partial mesh</em> connects only critical paths to balance cost and resilience. Common in WAN cores and wireless backhaul.</li><li><strong>Hybrid</strong> &mdash; any combination, such as a star-of-stars (tree) used in structured cabling, where workgroup switches uplink to a building distribution switch.</li></ul><h3>Counting full-mesh links</h3><p>The formula n(n-1)/2 grows quickly, which is why full mesh is reserved for small numbers of critical nodes. Four nodes need 4(3)/2 = 6 links; six nodes need 6(5)/2 = 15 links; ten nodes already need 45 links. Each added node n requires (n-1) new links, so cost scales roughly with the square of the node count. Partial mesh exists precisely to capture most of the redundancy benefit at a fraction of the cabling cost by meshing only the routers or sites that carry the most traffic.</p><h3>Datacenter and modern architectures</h3><p>Traditional enterprise design uses the <span class='keyterm'>three-tier hierarchy</span>: <strong>access</strong> (connects endpoints), <strong>distribution/aggregation</strong> (policy, routing between VLANs), and <strong>core</strong> (high-speed backbone). This north-south model suits client-server traffic, where most flows travel from an endpoint up to a server or out to the internet and back. Each tier has a clear job: the access layer applies port security and assigns VLANs, the distribution layer routes between VLANs and applies access control lists, and the core forwards as fast as possible with no policy overhead.</p><p>Datacenters increasingly use <span class='keyterm'>spine-and-leaf</span> (a Clos fabric). Every leaf (access) switch connects to every spine switch, but leaves never connect to each other and spines never connect to each other. This guarantees that any server is exactly two hops from any other server, delivering predictable low latency and easy horizontal scaling for the heavy <strong>east-west</strong> (server-to-server) traffic of virtualization and storage. To add capacity you simply add another spine (more bandwidth) or another leaf (more ports); existing devices are untouched. Equal-cost multipath routing spreads flows across all spines so no single link becomes a bottleneck.</p><table><thead><tr><th>Model</th><th>Traffic pattern</th><th>Hop predictability</th><th>Best for</th></tr></thead><tbody><tr><td>Three-tier</td><td>North-south</td><td>Varies by path</td><td>Campus/enterprise LANs</td></tr><tr><td>Spine-leaf</td><td>East-west</td><td>Always two hops</td><td>Modern datacenters</td></tr><tr><td>Collapsed core</td><td>Mixed</td><td>Fewer hops</td><td>Small sites (core+distribution merged)</td></tr></tbody></table><p>Two related concepts: <span class='keyterm'>SOHO</span> (small office/home office) networks collapse all tiers into one router/switch/AP combo, while a <span class='keyterm'>collapsed core</span> merges core and distribution for cost savings in smaller campuses where a dedicated core layer would be overkill.</p><h3>Common pitfalls</h3><p>Candidates frequently confuse physical and logical topology and answer for the wrong one; reread whether the question describes cabling or data flow. A second trap is assuming spine-leaf benefits north-south traffic &mdash; its whole purpose is east-west. A third is miscounting domains: a star built on a hub is one collision domain, while a star built on a switch is one collision domain per port. Finally, do not equate redundancy with mesh in every case; ring topologies also provide a self-healing alternate path, and the most common LAN choice remains the simple star.</p><div class='callout exam'><div class='lbl'>Exam Tip</div><p>If asked which topology gives the most redundancy, answer <strong>full mesh</strong>. If asked which is most common in today's LANs, answer <strong>star</strong>. If a datacenter question mentions east-west traffic or virtualization, the answer is <strong>spine-and-leaf</strong>. Remember full-mesh link count = n(n-1)/2 &mdash; six nodes need 15 links. When a question says control and forwarding are centralized or a fabric scales by adding identical switches, it is steering you toward spine-leaf, not three-tier.</p></div>"},
  {heading:"Network Types and Geographic Scope (LAN/WAN/MAN/CAN/PAN/SAN/WLAN)", body:"<p>Networks are categorized by the geographic area they span and the function they serve. The exam expects you to match a scenario to the correct acronym instantly, so anchor each type to a concrete mental image of distance and ownership. The key variables are how far the network reaches and who owns or operates the links &mdash; a single organization on private cable, or a carrier providing transport across public rights of way.</p><table><thead><tr><th>Type</th><th>Scope</th><th>Typical owner</th><th>Example</th></tr></thead><tbody><tr><td><strong>PAN</strong> (Personal Area Network)</td><td>~10 m, one person</td><td>Individual</td><td>Bluetooth earbuds, smartwatch tether</td></tr><tr><td><strong>LAN</strong> (Local Area Network)</td><td>One building/floor</td><td>Organization</td><td>Office Ethernet + Wi-Fi</td></tr><tr><td><strong>WLAN</strong> (Wireless LAN)</td><td>One building, wireless</td><td>Organization</td><td>802.11 Wi-Fi via APs</td></tr><tr><td><strong>CAN</strong> (Campus Area Network)</td><td>Group of nearby buildings</td><td>Organization (private fiber)</td><td>University or corporate campus</td></tr><tr><td><strong>MAN</strong> (Metropolitan Area Network)</td><td>City-wide</td><td>Provider or municipality</td><td>Municipal fiber, metro Ethernet</td></tr><tr><td><strong>WAN</strong> (Wide Area Network)</td><td>Country/global</td><td>Carrier</td><td>The internet, MPLS between sites</td></tr><tr><td><strong>SAN</strong> (Storage Area Network)</td><td>Datacenter block storage</td><td>Organization</td><td>Fibre Channel / iSCSI to disk arrays</td></tr></tbody></table><h3>Distinguishing the trickier ones</h3><p>A <span class='keyterm'>CAN</span> sits between a LAN and a MAN: multiple owned buildings within walking distance, connected by private fiber the organization controls. Because the organization owns the cabling and rights of way, there is no recurring carrier charge for transport. A <span class='keyterm'>MAN</span> spans a city and usually involves a service provider, because crossing public streets and other property generally requires carrier-owned infrastructure or leased dark fiber. A <span class='keyterm'>WAN</span> crosses cities, regions, or continents and almost always relies on carrier links such as MPLS, leased lines, or internet VPNs. The progression LAN &rarr; CAN &rarr; MAN &rarr; WAN is essentially a ladder of increasing distance and decreasing private ownership.</p><p>A <span class='keyterm'>SAN</span> is the odd one out &mdash; it is defined by <em>function</em>, not size. It provides high-speed, block-level access to consolidated storage so servers see remote disks as if they were local. Block-level means the server's operating system formats and manages the remote volume just like a physical drive, which is why SANs run on dedicated, low-latency fabrics. Do not confuse a SAN with NAS (file-level access over a normal LAN), where the storage device presents shared folders via SMB or NFS rather than raw disk blocks.</p><table><thead><tr><th>Aspect</th><th>SAN</th><th>NAS</th></tr></thead><tbody><tr><td>Access level</td><td>Block (raw volumes)</td><td>File (shares/folders)</td></tr><tr><td>Protocols</td><td>Fibre Channel, iSCSI, FCoE</td><td>SMB, NFS</td></tr><tr><td>Network</td><td>Dedicated storage fabric</td><td>Ordinary LAN</td></tr><tr><td>Appears to server as</td><td>Local disk</td><td>Network drive</td></tr></tbody></table><h3>Specialized and emerging types</h3><ul><li><strong>WMN</strong> (Wireless Mesh Network) &mdash; APs relay for each other to extend coverage without cabling each node, self-forming and self-healing as nodes join or fail.</li><li><strong>Industrial/OT networks</strong> &mdash; SCADA, ICS, Modbus, and similar control systems with strict reliability and segmentation needs, often air-gapped or tightly firewalled from IT networks.</li><li><strong>Overlay networks</strong> &mdash; logical networks (VPNs, VXLAN, SD-WAN) built on top of an underlay, decoupling the addressing and policy of the virtual network from the physical transport beneath it.</li></ul><h3>Common pitfalls</h3><p>The most frequent error is calling a multi-building campus a MAN; if the organization owns the fiber and the buildings are adjacent, it is a CAN. Another is treating WLAN as a separate scope from LAN &mdash; a WLAN is simply a LAN whose access medium is radio, and the two usually coexist in the same building. A third is conflating SAN and NAS: remember SAN is block-level on a dedicated fabric, NAS is file-level over the regular LAN. Finally, a PAN is bounded by a single person's immediate space, so a Bluetooth keyboard is a PAN even though it uses the same radio band as Wi-Fi.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>A hospital connects five buildings on its grounds with its own buried fiber, runs Wi-Fi inside each building, and links two storage arrays to its database servers over a dedicated Fibre Channel fabric. That is a <strong>CAN</strong> (the campus), containing <strong>WLANs</strong> (the Wi-Fi) and a <strong>SAN</strong> (the storage fabric). If the hospital then connects to a clinic across the state via a carrier circuit, that link is a <strong>WAN</strong>. Note how a single site layers several network types at once &mdash; matching each detail to its acronym is exactly what the exam tests.</p></div><div class='callout exam'><div class='lbl'>Exam Tip</div><p>Decide by two questions: how far does it reach, and who owns the transport? Owned fiber across adjacent buildings is a <strong>CAN</strong>; a provider crossing a city is a <strong>MAN</strong>; carrier links between cities form a <strong>WAN</strong>. The word \"block-level\" or \"appears as a local disk\" always points to a <strong>SAN</strong>, never NAS.</p></div>"},
  {heading:"IPv4 Addressing: Classes, Private Ranges, and Special Addresses", body:"<p>An IPv4 address is 32 bits, written as four dotted-decimal octets (0&ndash;255 each), giving roughly 4.3 billion addresses. Every address pairs with a <span class='keyterm'>subnet mask</span> that separates the network portion from the host portion. The mask is itself 32 bits of contiguous 1s (network) followed by 0s (host); a device performs a bitwise AND of its address and mask to learn its own network number and decide whether a destination is local or must be sent to the default gateway.</p><h3>Binary and the octet values</h3><p>Each octet represents eight bits with place values 128, 64, 32, 16, 8, 4, 2, 1. The largest value an octet can hold is 128+64+32+16+8+4+2+1 = 255, which is why no octet exceeds 255. Understanding this is essential because every subnetting and masking operation is really binary math hidden behind decimal notation. For example, the mask 255.255.255.0 is twenty-four 1s followed by eight 0s, which is exactly what /24 means.</p><h3>Legacy classful ranges</h3><table><thead><tr><th>Class</th><th>First octet</th><th>Default mask</th><th>Use</th></tr></thead><tbody><tr><td>A</td><td>1&ndash;126</td><td>/8 (255.0.0.0)</td><td>Very large networks</td></tr><tr><td>B</td><td>128&ndash;191</td><td>/16 (255.255.0.0)</td><td>Medium networks</td></tr><tr><td>C</td><td>192&ndash;223</td><td>/24 (255.255.255.0)</td><td>Small networks</td></tr><tr><td>D</td><td>224&ndash;239</td><td>n/a</td><td>Multicast</td></tr><tr><td>E</td><td>240&ndash;255</td><td>n/a</td><td>Experimental/reserved</td></tr></tbody></table><p>Note that 127 is skipped &mdash; the entire <strong>127.0.0.0/8</strong> block is reserved for loopback, with <strong>127.0.0.1</strong> as the canonical localhost address. The class boundaries fall on convenient first-bit patterns: Class A addresses start with bit 0, Class B with bits 10, Class C with bits 110, Class D with 1110, and Class E with 1111, which is how a router historically inferred the default mask before CIDR. Classful addressing is obsolete in practice (replaced by CIDR), but the ranges are still tested.</p><h3>RFC 1918 private ranges</h3><p>These blocks are never routed on the public internet and are reused inside every organization. They exist precisely so that NAT can let many internal hosts share a handful of public addresses. Memorize them exactly:</p><ul><li><strong>10.0.0.0/8</strong> &mdash; 10.0.0.0 to 10.255.255.255 (one Class A block, ~16.7M addresses)</li><li><strong>172.16.0.0/12</strong> &mdash; 172.16.0.0 to 172.31.255.255 (16 Class B blocks)</li><li><strong>192.168.0.0/16</strong> &mdash; 192.168.0.0 to 192.168.255.255 (256 Class C blocks)</li></ul><p>The /12 on the 172 range is the subtle one: a /12 mask is 255.240.0.0, so the second octet varies only across the 16 values 16 through 31. That is why 172.15.x.x (just below) and 172.32.x.x (just above) are public addresses, not private.</p><h3>Other special addresses</h3><ul><li><strong>APIPA / link-local: 169.254.0.0/16</strong> &mdash; auto-assigned when a host cannot reach a DHCP server. Seeing a 169.254.x.x address is a classic symptom of <em>DHCP failure</em>. APIPA hosts can communicate only with other APIPA hosts on the same segment and cannot reach a gateway or the internet.</li><li><strong>Loopback: 127.0.0.1</strong> &mdash; tests the local TCP/IP stack; a successful ping confirms the stack is installed and functioning before you suspect the network.</li><li><strong>Default route: 0.0.0.0</strong> &mdash; means \"this host\" or, as 0.0.0.0/0, \"all destinations,\" the catch-all route to the gateway.</li><li><strong>Limited broadcast: 255.255.255.255</strong> &mdash; reaches all hosts on the local segment and is never forwarded by routers.</li></ul><table><thead><tr><th>Special address</th><th>Range/value</th><th>Meaning</th></tr></thead><tbody><tr><td>Loopback</td><td>127.0.0.0/8</td><td>Local stack test</td></tr><tr><td>APIPA</td><td>169.254.0.0/16</td><td>DHCP failed, self-assigned</td></tr><tr><td>Limited broadcast</td><td>255.255.255.255</td><td>All hosts on this segment</td></tr><tr><td>Default route</td><td>0.0.0.0/0</td><td>All destinations</td></tr></tbody></table><h3>Common pitfalls</h3><p>The biggest trap is the 172.16.0.0/12 boundary, discussed above. A second is mistaking an APIPA address for a routing problem; a 169.254 address means the client never got a lease, so investigate DHCP reachability, the scope, or the relay agent. A third is forgetting that 127.x is loopback and therefore that pinging 127.0.0.1 says nothing about cabling or the gateway &mdash; it only proves the local stack works. Finally, remember Class D and E have no usable host masks; they are multicast and reserved respectively.</p><div class='callout exam'><div class='lbl'>Exam Tip</div><p>The 172.16.0.0/12 boundary is the most-missed fact. The private range is <strong>172.16.x.x through 172.31.x.x</strong> &mdash; so 172.15.x.x and 172.32.x.x are <em>public</em>. If a workstation shows <strong>169.254.x.x</strong>, do not blame the gateway first; the host failed to get a DHCP lease and assigned itself an APIPA address. APIPA hosts can talk only to other APIPA hosts on the same segment. And if ping 127.0.0.1 succeeds but ping of the gateway fails, the stack is fine and the problem is the link or configuration.</p></div>"},
  {heading:"Subnetting and CIDR Fundamentals", body:"<p><span class='keyterm'>CIDR</span> (Classless Inter-Domain Routing) replaced rigid classes with a flexible <em>prefix length</em>: the /n notation states how many leading bits are the network portion. The remaining bits are host bits. This lets administrators carve a block into right-sized subnets and lets the internet aggregate many routes into one summary, dramatically shrinking routing tables.</p><h3>The core formulas</h3><ul><li>Host bits h = 32 &minus; prefix length</li><li>Total addresses per subnet = 2^h</li><li><strong>Usable hosts = 2^h &minus; 2</strong> (subtract the network address and the broadcast address)</li><li>Number of equal subnets when borrowing b bits = 2^b</li></ul><p>The minus-two rule applies because the all-zeros host portion is the <span class='keyterm'>network address</span> and the all-ones host portion is the <span class='keyterm'>broadcast address</span> &mdash; neither can be assigned to a device. A /31 (RFC 3021 point-to-point) and /32 (single host) are the exceptions, where the minus-two rule is waived for transit links and host routes.</p><h3>Reading the mask and finding the block size</h3><p>The single most useful trick is the <span class='keyterm'>block size</span> (also called the increment): block size = 256 &minus; the interesting octet of the mask. The interesting octet is the last non-255 octet. Subnets always begin at multiples of the block size, and the broadcast is one less than the next subnet's network address. With this, you can solve almost any subnetting question without writing binary.</p><table><thead><tr><th>Prefix</th><th>Mask</th><th>Block size</th><th>Total</th><th>Usable hosts</th></tr></thead><tbody><tr><td>/24</td><td>255.255.255.0</td><td>256</td><td>256</td><td>254</td></tr><tr><td>/25</td><td>255.255.255.128</td><td>128</td><td>128</td><td>126</td></tr><tr><td>/26</td><td>255.255.255.192</td><td>64</td><td>64</td><td>62</td></tr><tr><td>/27</td><td>255.255.255.224</td><td>32</td><td>32</td><td>30</td></tr><tr><td>/28</td><td>255.255.255.240</td><td>16</td><td>16</td><td>14</td></tr><tr><td>/30</td><td>255.255.255.252</td><td>4</td><td>4</td><td>2</td></tr></tbody></table><h3>Worked example 1: splitting a /24 for 50 hosts</h3><p>Subnet <strong>192.168.1.0/24</strong> into networks supporting at least 50 hosts each. We need host bits where 2^h &minus; 2 &ge; 50, so h = 6 (62 hosts). That leaves a /26 (32 &minus; 6). Borrowing 2 bits from the /24 gives 2^2 = 4 subnets, each with block size 64:</p><ul><li><strong>192.168.1.0/26</strong> &mdash; network .0, usable .1&ndash;.62, broadcast .63</li><li><strong>192.168.1.64/26</strong> &mdash; network .64, usable .65&ndash;.126, broadcast .127</li><li><strong>192.168.1.128/26</strong> &mdash; network .128, usable .129&ndash;.190, broadcast .191</li><li><strong>192.168.1.192/26</strong> &mdash; network .192, usable .193&ndash;.254, broadcast .255</li></ul><p>The block size is 256 &minus; 192 = 64, which is why each subnet starts at a multiple of 64. To find any address's subnet, round its last octet down to the nearest multiple of the block size.</p><h3>Worked example 2: a /27 in the third octet idea, kept simple</h3><p>Take <strong>10.20.30.0/27</strong>. Host bits h = 32 &minus; 27 = 5, so 2^5 = 32 total and 30 usable, block size 32. The subnets in the fourth octet are .0, .32, .64, .96, .128, .160, .192, .224. For address <strong>10.20.30.70/27</strong>: 70 rounds down to 64, so the network is 10.20.30.64, the broadcast is .95 (one below the next block .96), and the usable range is .65 through .94. A host at .70 is valid; a host configured as .64 or .95 would be unusable because those are the network and broadcast addresses.</p><h3>Worked example 3: a /23 summarizing two /24s</h3><p>A <strong>/23</strong> (mask 255.255.254.0) has 9 host bits, giving 510 usable hosts across two contiguous /24 ranges. For 172.16.4.0/23 the block size in the third octet is 256 &minus; 254 = 2, so this subnet covers 172.16.4.0 through 172.16.5.255: network 172.16.4.0, broadcast 172.16.5.255, usable 172.16.4.1 through 172.16.5.254. This is how route summarization collapses several smaller networks into one advertisement.</p><h3>Common pitfalls</h3><p>The classic mistake is forgetting the minus-two rule and over-promising host counts; a /26 supports 62 usable hosts, not 64. Another is misidentifying the interesting octet on shorter prefixes like /23 or /22, where the block size lives in the third octet rather than the fourth. A third is assigning a host the network or broadcast address by accident. Practice the round-down-to-block-size technique until it is automatic, because most exam subnetting items are solved fastest that way rather than by drawing out binary.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>Host 192.168.1.100/26: the block size is 64, so 100 falls in the .64 subnet. Network = 192.168.1.64, broadcast = .127, valid host range .65&ndash;.126. The host is valid and its default gateway would typically be .65 or .126. If the same host were given a /25 mask instead, the block size would be 128, placing it in the .0 subnet (network .0, broadcast .127), which changes who counts as a local neighbor and which gateway it must use.</p></div>"},
  {heading:"Variable-Length Subnet Masking (VLSM) and Efficient Allocation", body:"<p>Classic subnetting splits a block into <em>equal-sized</em> pieces, which wastes addresses when subnets have very different host counts. <span class='keyterm'>VLSM</span> applies different masks to different subnets carved from the same block, matching each subnet's size to its actual need. VLSM requires classless routing protocols (OSPF, EIGRP, RIPv2, BGP) that carry the mask in updates, because each subnet now has a different prefix length and the receiving router cannot assume a classful default.</p><h3>The golden rule: allocate largest first</h3><p>Always satisfy the biggest subnet first, then work down. Starting small can fragment the address space and leave you unable to fit a large subnet later, because a large subnet must begin on a boundary that is a multiple of its own block size. By placing the largest block at the start of the address space and stepping down in size, each subsequent smaller block naturally lands on a valid boundary inside the leftover space.</p><h3>Worked VLSM example</h3><p>You own <strong>192.168.10.0/24</strong> and must support: LAN-A 100 hosts, LAN-B 50 hosts, LAN-C 25 hosts, plus two point-to-point WAN links of 2 hosts each. Work from largest to smallest, choosing the smallest mask whose usable count meets each need.</p><table><thead><tr><th>Subnet</th><th>Need</th><th>Mask</th><th>Block</th><th>Network</th><th>Range (usable)</th><th>Broadcast</th></tr></thead><tbody><tr><td>LAN-A</td><td>100</td><td>/25 (126 usable)</td><td>128</td><td>.0</td><td>.1&ndash;.126</td><td>.127</td></tr><tr><td>LAN-B</td><td>50</td><td>/26 (62 usable)</td><td>64</td><td>.128</td><td>.129&ndash;.190</td><td>.191</td></tr><tr><td>LAN-C</td><td>25</td><td>/27 (30 usable)</td><td>32</td><td>.192</td><td>.193&ndash;.222</td><td>.223</td></tr><tr><td>WAN-1</td><td>2</td><td>/30 (2 usable)</td><td>4</td><td>.224</td><td>.225&ndash;.226</td><td>.227</td></tr><tr><td>WAN-2</td><td>2</td><td>/30 (2 usable)</td><td>4</td><td>.228</td><td>.229&ndash;.230</td><td>.231</td></tr></tbody></table><p>Notice the efficiency: the WAN links use <strong>/30</strong> masks, giving exactly two usable addresses for the two router interfaces and wasting nothing. With fixed-length subnetting at /25, those tiny links would each consume 126 host addresses. After this allocation, 192.168.10.232 through .255 remain free for future growth &mdash; a full 24 addresses, enough for several more point-to-point links or another small LAN.</p><h3>Verifying no overlap</h3><p>A good habit is to confirm that each subnet's broadcast is exactly one less than the next subnet's network address: LAN-A ends at .127 and LAN-B begins at .128; LAN-B ends at .191 and LAN-C begins at .192; LAN-C ends at .223 and WAN-1 begins at .224; WAN-1 ends at .227 and WAN-2 begins at .228. The contiguous, gap-free sequence proves there is no overlap and no wasted block between allocations.</p><h3>Why /30 (and /31) for point-to-point links</h3><p>A serial or routed link between two routers needs only two host addresses. A /30 provides exactly that (4 total &minus; 2). Where supported, <strong>/31</strong> (RFC 3021) provides two usable addresses with no network/broadcast overhead, doubling efficiency on transit links by reclaiming the two addresses a /30 would otherwise burn. Across a large WAN with hundreds of links, moving from /30 to /31 frees a significant pool of addresses.</p><h3>Common pitfalls</h3><p>The number-one error is allocating a small subnet before a large one, which strands the large subnet because no remaining boundary fits it. The second is rounding host counts wrong: 100 hosts needs a /25 (126 usable), because a /26 gives only 62 &mdash; never choose a mask whose usable count is below the requirement. The third is forgetting that VLSM needs a classless routing protocol; configuring it under classful RIPv1 will silently break. Always sort, size, and then lay blocks contiguously while checking that broadcasts and networks abut perfectly.</p><div class='callout exam'><div class='lbl'>Exam Tip</div><p>VLSM questions are solved by: (1) sort subnets largest to smallest, (2) pick the smallest mask that fits each, (3) assign blocks contiguously without overlap. Watch for the trap of assigning a small subnet first and then having no room for the big one. Remember 100 hosts needs a /25 (not /26, which only gives 62), and any point-to-point link should be a /30 (or /31 if the question stresses maximum efficiency).</p></div>"},
  {heading:"Network Address Translation (NAT and PAT)", body:"<p><span class='keyterm'>NAT</span> rewrites IP address information in packet headers as traffic crosses a router, allowing privately addressed (RFC 1918) hosts to communicate over the public internet. It was the primary stopgap for IPv4 exhaustion and remains ubiquitous at every home and office edge. NAT operates at the boundary between the inside (private) and outside (public) networks, maintaining a table that lets it reverse the translation for return traffic so replies find their way back to the correct internal host.</p><h3>Flavors of NAT</h3><ul><li><strong>Static NAT</strong> &mdash; a one-to-one permanent mapping of one private address to one public address. Used to publish an internal server (for example, a web server) at a fixed public IP so external clients can always reach it at the same address.</li><li><strong>Dynamic NAT</strong> &mdash; private hosts draw from a pool of public addresses on demand. Once the pool is exhausted, new hosts are blocked, so dynamic NAT still requires roughly as many public addresses as concurrent inside hosts.</li><li><strong>PAT (Port Address Translation) / NAT overload</strong> &mdash; many private hosts share a <em>single</em> public IP, distinguished by unique source port numbers. This is what virtually every home router does and is the reason a household of devices needs only one ISP-assigned public address.</li></ul><h3>How PAT works step by step</h3><p>When an inside host sends a packet, the NAT device records the inside private IP and port, then rewrites the source to the public IP plus a unique translated port. It stores this in a <span class='keyterm'>translation table</span>. Return traffic arrives at the public IP and translated port; the device looks up the entry and rewrites the destination back to the original private socket. Concretely, suppose 192.168.1.10:51000 and 192.168.1.11:51000 both browse the web. The router might map the first to 203.0.113.5:40001 and the second to 203.0.113.5:40002. Even though both inside hosts happened to use the same source port, the unique translated ports keep the two sessions distinct. Because a port field is 16 bits, one public IP can theoretically track tens of thousands of simultaneous sessions.</p><table><thead><tr><th>Type</th><th>Mapping</th><th>Public IPs needed</th><th>Typical use</th></tr></thead><tbody><tr><td>Static NAT</td><td>1 private : 1 public (fixed)</td><td>One per published host</td><td>Inbound to internal server</td></tr><tr><td>Dynamic NAT</td><td>many : pool</td><td>Pool sized to concurrency</td><td>Limited outbound</td></tr><tr><td>PAT</td><td>many : 1 (by port)</td><td>One total</td><td>Home/office internet access</td></tr></tbody></table><h3>Terminology and side effects</h3><p>Cisco terms: <strong>inside local</strong> (private source), <strong>inside global</strong> (translated public), <strong>outside global</strong> (public destination), and <strong>outside local</strong> (how the outside host appears inside). NAT inadvertently provides a measure of security by hiding internal topology, but it is <em>not</em> a firewall &mdash; it makes no policy decision about whether traffic should be allowed, only how addresses are rewritten. It also breaks protocols that embed IP addresses in the payload (FTP active mode, SIP, IPsec AH) &mdash; these need ALGs (application-layer gateways) or NAT traversal techniques like STUN/TURN/ICE. <strong>Port forwarding</strong> (a static PAT entry) maps a chosen public port to a specific inside host and port so unsolicited inbound connections can reach a server behind PAT.</p><h3>Common pitfalls</h3><p>A frequent misconception is that NAT alone secures a network; it obscures addresses but enforces no rules, so a firewall is still required. Another is confusing dynamic NAT with PAT &mdash; dynamic NAT still needs a pool of public addresses, while PAT collapses everyone onto one address using ports. A third is forgetting that protocols embedding addresses in the payload may fail without an ALG, which explains mysterious VoIP or active-FTP breakage behind NAT. Finally, remember static NAT and port forwarding are for inbound reachability, whereas PAT is fundamentally an outbound-sharing mechanism.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>A home of twelve devices browses the internet simultaneously through one ISP-assigned public address. The router uses <strong>PAT</strong>, tracking each connection by a unique source port. To let an outside user reach an internal game server, the admin configures <strong>port forwarding</strong> (a static PAT entry) mapping a public port to the server's private IP and port. If a second game server needs the same internal port, the admin simply forwards a different public port to it, since the inside host and port together make each mapping unique.</p></div><div class='callout exam'><div class='lbl'>Exam Tip</div><p>When a question describes many hosts sharing one public IP, the answer is <strong>PAT/NAT overload</strong>. When it describes a fixed public IP permanently mapped to one internal server, that is <strong>static NAT</strong>. Inbound reachability to a server behind PAT means <strong>port forwarding</strong>. And remember NAT is not a security control by itself &mdash; do not select it as the answer to \"what filters traffic.\"</p></div>"},
  {heading:"IPv6 Addressing: Structure, Types, and SLAAC", body:"<p>IPv6 uses <strong>128-bit</strong> addresses, written as eight groups of four hexadecimal digits separated by colons, yielding ~340 undecillion addresses. Two compression rules: drop leading zeros in a group, and replace <em>one</em> run of all-zero groups with a double colon (<strong>::</strong>). Thus 2001:0db8:0000:0000:0000:ff00:0042:8329 becomes 2001:db8::ff00:42:8329. The double colon may appear only once in an address, because two of them would make the number of restored zero groups ambiguous.</p><h3>Worked compression example</h3><p>Take fe80:0000:0000:0000:0204:61ff:fe9d:f156. First drop leading zeros in each group: fe80:0:0:0:204:61ff:fe9d:f156. Then collapse the longest single run of zero groups with ::, giving fe80::204:61ff:fe9d:f156. To expand a compressed address, count the groups present and pad the :: back out to reach eight groups total; if an address shows five explicit groups and a ::, the :: stands for three zero groups.</p><h3>Address structure</h3><p>A typical global address splits into a <strong>/64</strong> boundary: the first 64 bits are the network prefix (routing) and the last 64 bits are the <span class='keyterm'>interface identifier</span> (host). Within the prefix, providers typically delegate a <strong>/48</strong> to a site and the site assigns <strong>/64</strong> subnets, leaving 16 bits of subnet space (65,536 possible /64 subnets). The /64 host block is enormous by design &mdash; 2^64 addresses per subnet &mdash; which is what enables stateless autoconfiguration and removes any practical need to conserve host addresses.</p><h3>IPv6 address types</h3><table><thead><tr><th>Type</th><th>Prefix</th><th>Role</th></tr></thead><tbody><tr><td>Global unicast</td><td>2000::/3</td><td>Public, internet-routable</td></tr><tr><td>Link-local</td><td>fe80::/10</td><td>Single link only; auto-generated on every interface</td></tr><tr><td>Unique local (ULA)</td><td>fc00::/7 (fd00::/8 used)</td><td>Private, like RFC 1918</td></tr><tr><td>Multicast</td><td>ff00::/8</td><td>One-to-many delivery</td></tr><tr><td>Loopback</td><td>::1</td><td>Equivalent to 127.0.0.1</td></tr><tr><td>Unspecified</td><td>::</td><td>No address yet (all zeros)</td></tr></tbody></table><p>IPv6 has <strong>no broadcast</strong>. Its job is done by multicast groups such as <strong>ff02::1</strong> (all nodes) and <strong>ff02::2</strong> (all routers). Every interface always has a link-local address used for neighbor discovery and routing protocol exchanges, so even a host with no global address can still talk to neighbors on its own link. Neighbor Discovery Protocol (NDP) replaces IPv4's ARP, using ICMPv6 neighbor solicitation and advertisement messages to resolve addresses.</p><h3>SLAAC and EUI-64</h3><p><span class='keyterm'>SLAAC</span> (Stateless Address Autoconfiguration) lets a host build its own global address without a DHCP server. The router advertises the /64 prefix via an <strong>ICMPv6 Router Advertisement (RA)</strong>; the host appends a self-generated 64-bit interface ID to form a complete address. The host may also send a Router Solicitation to prompt an immediate RA rather than waiting for the next periodic one.</p><p>The classic interface ID method is <span class='keyterm'>EUI-64</span>: take the 48-bit MAC, split it in half, insert <strong>FFFE</strong> in the middle, and flip the 7th bit (the universal/local bit). For example, MAC 00:24:17:9d:f1:56 becomes interface ID 0224:17ff:fe9d:f156 &mdash; note FFFE inserted in the center and the first byte changed from 00 to 02 by flipping the U/L bit. For privacy, most modern OSes instead use randomized interface IDs (RFC 4941/7217) rather than exposing the MAC, since EUI-64 would let remote sites track a device by its embedded hardware address.</p><table><thead><tr><th>Assignment method</th><th>Needs DHCPv6?</th><th>How the host gets its address</th></tr></thead><tbody><tr><td>SLAAC</td><td>No</td><td>RA prefix + self-generated interface ID</td></tr><tr><td>Stateful DHCPv6</td><td>Yes</td><td>Server leases full address and tracks it</td></tr><tr><td>Stateless DHCPv6</td><td>Partial</td><td>SLAAC for address, DHCPv6 for DNS/options</td></tr></tbody></table><h3>Common pitfalls</h3><p>A common slip is using :: more than once, which is invalid. Another is forgetting that link-local fe80:: addresses are not routable and exist on every interface regardless of global addressing, so a host can have a working link-local address yet no internet connectivity. A third is assuming IPv6 needs DHCP &mdash; SLAAC means many networks assign global addresses with no DHCP server at all. Finally, do not expect broadcast in IPv6; tasks that used broadcast in IPv4 (like ARP) use multicast groups here.</p><div class='callout exam'><div class='lbl'>Exam Tip</div><p>Memorize the prefixes: link-local <strong>fe80::/10</strong>, multicast <strong>ff00::/8</strong>, unique local <strong>fc00::/7</strong>, global unicast <strong>2000::/3</strong>, loopback <strong>::1</strong>. If a question says a host configured an IPv6 address from a Router Advertisement with no DHCP, that is <strong>SLAAC</strong>. EUI-64 is recognizable by the <strong>FFFE</strong> inserted in the middle of the interface ID. Remember IPv6 has no broadcast &mdash; ff02::1 reaches all nodes instead.</p></div>"},
  {heading:"IPv6 Transition: Dual Stack and Tunneling", body:"<p>The internet cannot migrate from IPv4 to IPv6 overnight, so several coexistence mechanisms let both protocols operate during the long transition. Network+ focuses on dual stack, tunneling, and translation, and the exam usually wants you to pick the right one given a constraint such as \"the transit network is IPv4-only\" or \"both ends support IPv6.\"</p><h3>Dual stack</h3><p><span class='keyterm'>Dual stack</span> runs IPv4 and IPv6 simultaneously on the same interfaces. The host holds both an IPv4 and an IPv6 address and chooses the appropriate protocol per destination &mdash; typically preferring IPv6 when a AAAA DNS record exists, falling back to IPv4 (A record) otherwise. This selection logic (often called Happy Eyeballs) races both and uses whichever responds first, smoothing the user experience. Dual stack is the <strong>preferred</strong> and least error-prone migration approach because it requires no encapsulation, but it demands that every device and the network path support both stacks, and it doubles the addressing and firewall policy you must maintain.</p><h3>Tunneling</h3><p>Tunneling carries IPv6 packets across an IPv4-only network (or vice versa) by encapsulating one protocol inside the other. The original packet becomes the payload of a carrier packet, and the endpoints strip the outer header to recover it. Key technologies:</p><ul><li><strong>6to4</strong> &mdash; encapsulates IPv6 in IPv4 (protocol 41) using a 2002::/16 prefix derived from the public IPv4 address. No explicit tunnel configuration needed.</li><li><strong>6in4 / manual tunnels</strong> &mdash; statically configured point-to-point IPv6-over-IPv4 tunnels between known endpoints; predictable but administratively heavy.</li><li><strong>Teredo</strong> &mdash; tunnels IPv6 through IPv4 <em>and through NAT</em> by encapsulating in UDP; a last resort for hosts behind NAT with no native IPv6.</li><li><strong>ISATAP</strong> &mdash; treats an IPv4 intranet as a virtual IPv6 link layer for intra-site connectivity.</li><li><strong>GRE</strong> &mdash; a generic encapsulation that can also carry IPv6 across IPv4.</li></ul><table><thead><tr><th>Mechanism</th><th>What it does</th><th>When to use</th></tr></thead><tbody><tr><td>Dual stack</td><td>Both protocols natively</td><td>Preferred; gear supports both</td></tr><tr><td>Tunneling</td><td>Encapsulate one in the other</td><td>Cross an incompatible transit network</td></tr><tr><td>NAT64 / DNS64</td><td>Translate v6 to v4</td><td>IPv6-only host reaching IPv4-only server</td></tr></tbody></table><h3>Translation</h3><p>When endpoints speak <em>different</em> protocols, translation is required because encapsulation alone cannot help an IPv6-only host talk to an IPv4-only server. <span class='keyterm'>NAT64</span> paired with <span class='keyterm'>DNS64</span> lets an IPv6-only client reach IPv4-only services: DNS64 synthesizes a AAAA record pointing at a NAT64 prefix, and the NAT64 gateway rewrites between the two protocols, swapping the IPv6 header for an IPv4 header on the way out and back. This is increasingly common in mobile carrier networks that run IPv6-only internally.</p><h3>Choosing the right mechanism</h3><p>Decide by what the path and the endpoints support. If every device and link supports both protocols, use dual stack &mdash; it is native and simplest to reason about. If the two IPv6 islands are separated by an IPv4-only transit, tunnel across it. If one side can speak only IPv4 and the other only IPv6, you must translate with NAT64/DNS64. Teredo is the specific keyword whenever the obstacle is NAT.</p><h3>Common pitfalls</h3><p>The recurring trap is choosing tunneling when dual stack would work; tunneling is only for crossing an incompatible transit, not a default. Another is confusing tunneling with translation &mdash; tunneling wraps the same protocol family inside another for transport, while translation actually converts between IPv4 and IPv6. A third is forgetting that Teredo specifically solves the NAT problem, so any question mentioning hosts stuck behind NAT with no native IPv6 points to Teredo. Finally, remember dual stack means maintaining two sets of firewall rules, which is an operational cost worth noting.</p><div class='callout exam'><div class='lbl'>Exam Tip</div><p>If the question asks for the <strong>recommended</strong> or <strong>simplest reliable</strong> transition method and the hardware supports both protocols, choose <strong>dual stack</strong>. Choose <strong>tunneling</strong> only when you must move one protocol across a transit network that does not support it. Teredo is the keyword for tunneling specifically through <strong>NAT</strong>. NAT64/DNS64 is for connecting an IPv6-only side to an IPv4-only side.</p></div>"},
  {heading:"Cloud Concepts and Modern Network Use Cases", body:"<p>Modern infrastructure blends on-premises networks with cloud platforms and software-defined control. Network+ tests the service and deployment models plus a cluster of contemporary acronyms, and it expects you to reason about who is responsible for what under each model &mdash; the so-called shared responsibility principle.</p><h3>Cloud service models</h3><table><thead><tr><th>Model</th><th>You manage</th><th>Provider manages</th><th>Example</th></tr></thead><tbody><tr><td><strong>IaaS</strong></td><td>OS, apps, data</td><td>Compute, storage, network, virtualization</td><td>AWS EC2, Azure VMs</td></tr><tr><td><strong>PaaS</strong></td><td>Your application and data</td><td>OS, runtime, middleware, hardware</td><td>App Engine, Azure App Service</td></tr><tr><td><strong>SaaS</strong></td><td>Only your data/settings</td><td>Everything else end to end</td><td>Microsoft 365, Salesforce</td></tr></tbody></table><p>The simple mental model: with <strong>IaaS</strong> you rent the building blocks, with <strong>PaaS</strong> you rent the runtime, with <strong>SaaS</strong> you just log in and use the app. Responsibility shifts to the provider as you move down the list; with SaaS your only real job is managing users, data, and configuration, while with IaaS you still patch the operating system and secure the application yourself.</p><h3>Deployment models</h3><ul><li><strong>Public</strong> &mdash; shared provider infrastructure (multi-tenant); lowest cost, highest elasticity.</li><li><strong>Private</strong> &mdash; dedicated to one organization; greater control and isolation at higher cost.</li><li><strong>Hybrid</strong> &mdash; combines public and private, often bridged by VPN or direct connect, letting an organization keep sensitive workloads private while bursting to public capacity on demand.</li><li><strong>Community</strong> &mdash; shared by organizations with common requirements, such as agencies under the same compliance regime.</li></ul><p>Inside a public cloud, your isolated logical network is a <span class='keyterm'>VPC</span> (Virtual Private Cloud) &mdash; you define subnets, route tables, security groups, and gateways just as you would on-premises. Connectivity back to your datacenter is typically a site-to-site VPN over the internet or a dedicated private circuit (AWS Direct Connect, Azure ExpressRoute) for lower, more predictable latency.</p><h3>Software-defined and secure-access concepts</h3><ul><li><strong>SDN</strong> (Software-Defined Networking) &mdash; separates the <em>control plane</em> (decision-making) from the <em>data plane</em> (forwarding), centralizing policy in a controller programmable via APIs. This lets administrators push consistent policy across many devices from one place instead of configuring each box by hand.</li><li><strong>SD-WAN</strong> &mdash; applies SDN principles to WAN links, intelligently steering traffic across MPLS, broadband, and LTE based on application policy, and failing over automatically when a link degrades.</li><li><strong>SASE</strong> (Secure Access Service Edge) &mdash; converges SD-WAN with cloud-delivered security (SWG, CASB, FWaaS, ZTNA) into one service, inspecting traffic in the cloud close to the user rather than backhauling it to a central datacenter. <strong>SSE</strong> is the security-only subset of SASE.</li><li><strong>Zero Trust</strong> &mdash; \"never trust, always verify\"; authenticate and authorize every request regardless of network location, enforcing least privilege and microsegmentation so a breach in one segment cannot spread laterally.</li></ul><table><thead><tr><th>Concept</th><th>Core idea</th><th>Primary benefit</th></tr></thead><tbody><tr><td>SDN</td><td>Central controller, API-driven</td><td>Consistent, programmable policy</td></tr><tr><td>SD-WAN</td><td>Policy-based multi-link steering</td><td>Resilient, cost-effective WAN</td></tr><tr><td>SASE</td><td>SD-WAN + cloud security converged</td><td>Secure access from anywhere</td></tr><tr><td>Zero Trust</td><td>Verify every request, least privilege</td><td>Limits lateral movement</td></tr></tbody></table><h3>Automation and infrastructure as code</h3><p><span class='keyterm'>IaC</span> (Infrastructure as Code) defines networks and servers in declarative text (Terraform, Ansible, CloudFormation) so environments are version-controlled, repeatable, and rapidly deployable. Because the desired state lives in code, a whole environment can be rebuilt identically from the same template, eliminating configuration drift and manual error. This underpins elastic <em>scalability</em> and <em>rapid elasticity</em>, two defining cloud characteristics alongside on-demand self-service, broad network access, resource pooling, and measured (pay-per-use) billing.</p><h3>Common pitfalls</h3><p>Candidates often blur the service models; anchor on what you manage &mdash; OS means IaaS, just your app means PaaS, just log in means SaaS. Another mix-up is SASE versus SSE: SASE includes the SD-WAN networking component, while SSE is the security-only piece. A third is treating Zero Trust as a single product rather than an architecture and mindset of verifying every request. Finally, do not confuse deployment models (public/private/hybrid/community) with service models (IaaS/PaaS/SaaS); a question may combine both, such as a private PaaS.</p><div class='callout scenario'><div class='lbl'>Scenario</div><p>A retailer connects 200 stores with <strong>SD-WAN</strong> for resilient, policy-based links, layers <strong>SASE</strong> so each store's traffic is inspected by cloud security on the way to <strong>SaaS</strong> apps, enforces <strong>Zero Trust</strong> so a compromised POS cannot move laterally, and provisions every new store's <strong>VPC</strong> subnets and firewall rules from <strong>IaC</strong> templates in minutes rather than days. Each acronym maps to a distinct need: SD-WAN for transport, SASE for cloud-edge security, Zero Trust for containment, and IaC for repeatable deployment.</p></div><div class='callout exam'><div class='lbl'>Exam Tip</div><p>Decide service models by responsibility: manage the OS = <strong>IaaS</strong>, manage only the app = <strong>PaaS</strong>, manage only data = <strong>SaaS</strong>. SASE = SD-WAN + cloud security; <strong>SSE</strong> is the security-only subset. Zero Trust is an architecture (verify everything, least privilege, microsegmentation), not a single appliance. A logical isolated network inside a public cloud is a <strong>VPC</strong>.</p></div>"}
);
